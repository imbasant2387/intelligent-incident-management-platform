apiVersion: v1
kind: Service

metadata:
  name: user-service
  namespace: iimp

  labels:
    app: user-service

spec:
  selector:
    app: user-service

  ports:
  - name: http
    port: 8000
    targetPort: 8000
